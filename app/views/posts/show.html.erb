<p style="color: green"><%= notice %></p>

<%= render @post %>

<div>
  <%= link_to t('.edit'), edit_post_path(@post) %> |
  <%= link_to t('.back'), posts_path %>

  <%= button_to t('.destroy'), @post, method: :delete, data: { confirm: t('.confirm_destroy') } %>
</div>